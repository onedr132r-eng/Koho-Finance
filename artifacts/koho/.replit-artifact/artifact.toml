kind = "web"
previewPath = "/"
title = "KOHO Fintech"
version = "1.0.0"
id = "artifacts/koho"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24354

[services.development]
run = "pnpm --filter @workspace/koho run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/koho", "run", "build" ]
publicDir = "artifacts/koho/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24354"
BASE_PATH = "/"
